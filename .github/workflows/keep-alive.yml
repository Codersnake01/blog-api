name: Keep-Alive

on:
  schedule:
    - cron: "0 */6 * * *"  # Cada 6 horas
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Blog API
        run: curl -s -o /dev/null -w "%{http_code}" https://blog-api-q3s5.onrender.com